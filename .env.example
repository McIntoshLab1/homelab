A file listing the environment variables the project needs, with placeholder values instead of real secrets.

# Cloudflare Tunnel (Phase 2 - public publishing)
CLOUDFLARE_TUNNEL_TOKEN=changeme

# Tailscale (used by Ansible to join VMs to the mesh)
TAILSCALE_AUTHKEY=changeme

# Ansible
ADMIN_USER=changeme
